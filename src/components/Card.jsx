import styled from "styled-components";

const CardContainer = styled.div``;

const Card = () => {
  return (
    <CardContainer>
      <h1>Hello World</h1>
    </CardContainer>
  );
};

export default Card;
